// Ambiente: produção (servidor)
window.__APP_CONFIG__ = {
  // Recomendado com Nginx/Apache fazendo proxy reverso:
  // - "/api" (mesma origem do site)
  API_BASE_URL: "/api",
  // Google OAuth Client ID (PROD)
  GOOGLE_CLIENT_ID: "",
};
